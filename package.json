{
  "name": "web3-dashboard-data",
  "version": "1.0.0",
  "description": "Fetches real-time data from Base blockchain",
  "main": "fetch-data.js",
  "scripts": {
    "fetch": "node fetch-data.js",
    "test": "node fetch-data.js --test"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0"
  }
}
